The reroll buttons are working, but I also want a button above all of them that generates an entire loadout, not just the reroll buttons. It should roll every different field.

-----------------------------------------------------------------------------

The generate loadout button at the top of the app does not generate anything. This is incorrect, it should roll all four sections for new values.

-----------------------------------------------------------------------------